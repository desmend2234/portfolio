module.exports = {
    extends: ['react-app', 'react-app/jest'],
    parser: '@babel/eslint-parser',
    parserOptions: {
        requireConfigFile: false,
    },
    rules: {
        // 在這裡添加你的自定義規則
    },
}
